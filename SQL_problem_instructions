

-Export the results out to a CSV (with headers on, and in CSV mode)
-Human explanation of what the table contains included

All queries containing calculations or functions within the select statements are renamed accordingly

All queries have at least one join
All queries are selecting specific columns by name

These are the four types of SQL statements:

Statement containing modified a numerical value as part of the select statement

Statement containing concatenated text data within the select statement

Statement where 3 three tables are joined 

Statement where data is aggregated using group by with multiple groups


